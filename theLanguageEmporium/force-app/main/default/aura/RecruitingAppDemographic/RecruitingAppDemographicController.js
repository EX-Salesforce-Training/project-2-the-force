({
    cDemographicPrevious: function (cmp, evt, hlp){
        hlp.hDemographicPrevious (cmp);		
    },
    
    cDemographicNext: function (cmp, evt, hlp){
        hlp.hDemographicNext (cmp);		
    },
    
    cRenderDemographic : function(cmp, evt, hlp){
        hlp.hRenderDemographic (cmp);
    },
    
    cHandleGender: function (cmp, evt, hlp) {
        hlp.hHandleGender (cmp);
    },
    
    cHandleEthnicity: function (cmp, evt, hlp) {
        hlp.hHandleEthnicity (cmp);
    },
    
    cHandleVeteran: function(cmp, evt, hlp) {
        hlp.hHandleVeteran (cmp);
    },
    
    cHandleDisability: function(cmp, evt, hlp) {
        hlp.hHandleDisability (cmp);
    }
    
});