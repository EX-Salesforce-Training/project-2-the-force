({
    navHome : function(component, event, helper) {
        helper.renderHomePage(component);

    },
    navAccount : function(component, event, helper) {
        helper.renderAccountPage(component);

    },
    navShop : function(component, event, helper) {
        helper.renderShopPage(component);

    },
    navCareers : function(component, event, helper) {
        helper.renderCareersPage(component);

    }
})
