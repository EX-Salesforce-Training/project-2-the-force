({
    cCandidateInputPrevious: function (cmp, evt, hlp){
        hlp.hHandleSubmit (cmp);		
    },
    
    cCandidateInputNext: function (cmp, evt, hlp){
        hlp.hHandleSubmit (cmp);		
    },
    
    cRenderCandidateInput : function(cmp, evt, hlp){
        hlp.hRenderCandidateInput (cmp);
    }
})