({
    cOpenPositions: function (cmp, evt, hlp){
        hlp.hOpenPositions (cmp);
    },
    
    cOnChange: function (cmp, evt, hlp){
        hlp.hOnChange (cmp);
    },
    
    cPositionsNext: function (cmp, evt, hlp){
        hlp.hPositionsNext (cmp);
    },
    
    cRenderPositions : function(component, event, helper){
        hlp.hRenderPositions (cmp);
    }
    
    
});